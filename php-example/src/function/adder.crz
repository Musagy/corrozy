fn autoAdder(): int {
  let a: int = 5;
  let b: int = 10;
  
  return a + b;
}

fn adder(
  a: int,
  b: int
): int {
  let int1: int = a;
  let int2: int = b;

  fn idk(): int {
    let result = int1 + int2;
    return result;
  }
  
  return idk();
}
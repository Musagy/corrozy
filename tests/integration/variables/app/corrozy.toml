[transpiler]
output_dir = "dist"       # can use output or dist
src_dir = "./src"        # can use ./ or ./src
strict_types = true
include_comments = true

[namespace]
mode = "auto"  # "auto" | "manual" | "none"
separator = "."  # "." | "\"
base_namespace = "MyApp"